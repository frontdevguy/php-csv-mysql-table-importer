# CSV to MySQL Table Importer

## Setting Up

Update database details in `config.php`

Update constants in `index.php` if needed

`cp orders.sample.csv orders.csv`

## Running Application

Run the `index.php` file in a php server
